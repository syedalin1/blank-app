[project]
name = "blank-app-template"
version = "0.1.0"
description = "Amelle TikTok Analytics App"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "streamlit",
    "pandas",
    "openpyxl",
]

[tool.uv]
package = false
